<Window x:Class="WPF_Labs.Windows.Third"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WPF_Labs.Windows"
        xmlns:wpfLabs="clr-namespace:WPF_Labs"
        mc:Ignorable="d"
        Title="Third" Height="450" Width="800">
    <Window.Resources>
        <wpfLabs:Product x:Key="product" Title="Sling VVC2m" Company="Genesis" Price="25000" />
    </Window.Resources>
    <Grid DataContext="{StaticResource product}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="Model" />
        <TextBlock Text="{Binding Title}" Grid.Row="1" />
        <TextBlock Text="Manufacturer" Grid.Column="1"/>
        <TextBlock Text="{Binding Company}" Grid.Column="1" Grid.Row="1" />
        <TextBlock Text="Price" Grid.Column="2" />
        <TextBlock Text="{Binding Price}" Grid.Column="2" Grid.Row="1" />
 
        <Button  Content="Change" Click="ButtonBase_OnClick" Grid.Column="1" Grid.Row="2" />
    </Grid>
</Window>
